export const metadata = {
    title: 'Login | Split Wise',
    description: 'Welcome, login to enter Split Wise',
  }
  
  export default function RootLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return (children)
  }